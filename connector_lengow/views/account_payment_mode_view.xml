<?xml version ="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_mode_view_form" model="ir.ui.view">
      <field name="model">account.payment.mode</field>
      <field name="inherit_id" ref="connector_ecommerce.account_payment_mode_form_inherit"/>
      <field name="arch" type="xml">

        <group name="import_rule" position="attributes">
          <attribute name='attrs'>{'invisible': [('marketplace_id', '!=', False)]}</attribute>
        </group>

        <field name='name'  position="attributes">
          <attribute name='attrs'>{'readonly': [('marketplace_id', '!=', False)]}</attribute>
        </field>

        <field name='company_id'  position="attributes">
          <attribute name='attrs'>{'readonly': [('marketplace_id', '!=', False)]}</attribute>
        </field>

        <field name='company_id' position="before">
          <field name='marketplace_id' readonly="1"/>
        </field>

      </field>
    </record>

</odoo>

